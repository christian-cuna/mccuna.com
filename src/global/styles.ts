export { appColors, appBorderStyle };

const appColors = {
  veryLightGray: '#eee',
  lightGray: '#ccc',
};

const appBorderStyle = {
  containerBorder: `1px solid ${appColors.lightGray}`,
};
